// return the image size in % of two elements based on an arbitrary width/height for each
// takes into consideration the width and height of the viewport so that both elements can fit side by side

relativeScaling = {
  getSizes: function(el1, el2, viewportWidth, veiwportHeight){

  }
}
